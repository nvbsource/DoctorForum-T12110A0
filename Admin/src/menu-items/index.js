import dashboard from './dashboard';
import account from './account';
import topic from './topic';
import post from './post';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
   items: [dashboard, account, topic, post]
};

export default menuItems;
